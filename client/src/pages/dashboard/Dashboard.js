import React, { useState } from 'react';

function Dashboard(props) {
    return (
        <React.Fragment>
            <h1>Dashboard</h1>
        </React.Fragment>
    );
}

export default Dashboard;
